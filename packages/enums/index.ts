// packages/types/index.ts

export enum AccountRole {
  USER = "user",
  ADMIN = "admin",
  MODERATOR = "moderator",
}

export enum AccountStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
  BANNED = "banned",
}
